name: Conditional CI Pipeline
on:
    push:

jobs:
  build:                       
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "Build completed!"

  test:                         
    runs-on: ubuntu-latest
    needs: build                
    if: startsWith(github.ref, 'refs/heads/feature/') && success()
    steps:
      - uses: actions/checkout@v3
      - run: echo "Running tests for feature branch!"